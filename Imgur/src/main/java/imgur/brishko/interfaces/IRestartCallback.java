package imgur.brishko.interfaces;

/**
 * Interface so that we can implement callback restart activity to non activity classes
 */
public interface IRestartCallback {
    public void restartActivity();
}
